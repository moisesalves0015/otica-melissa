rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for site assets and product images
      allow read: if true;
      // Restrict write (upload/delete) to authenticated administrators only
      allow write: if request.auth != null;
    }
  }
}
